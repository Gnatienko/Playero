import React from "react"
import "./Legend.css"

const Legend = () => {
  return (
    <span className="legend">
      &nbsp;&nbsp;&nbsp; Z, T - translate replica, &nbsp;&lt; - previous
      replica, &nbsp;&gt; - next replica
    </span>
  )
}

export default Legend
